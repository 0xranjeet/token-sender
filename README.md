# ERC20 Token Sender
Token Sender helps mod and event organizers send rewards easily using a smart contract

- Connect your wallet
- Load `owner`, `Token`, and `contractBalance`
- Deposit tokens with `approve + deposit`
- Send rewards using `sendReward`
